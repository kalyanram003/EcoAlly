# EcoLearn EcoQuest Screens

This is a local React application for EcoLearn EcoQuest.

## Running the project

1. Run `npm install` to install dependencies.
2. Run `npm start` to start the development server.